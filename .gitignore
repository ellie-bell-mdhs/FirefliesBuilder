node_modules/
dist/
.env
*.log
.DS_Store

# Auth token cache written at runtime (never commit tokens)
.tokens/

# Local scratch; keep the folder, ignore generated meeting output by default.
# Comment the next line out if you want generated builds tracked in git.
builds/
logs/
